class Solution:
    def maximumCount(self, nums: List[int]) -> int:
        n=len(nums)
        left=0
        right=n-1

        while left<=right:
            mid=(left+right)//2
            if nums[mid]<0:
                left=mid+1
            else:
                right=mid-1
                n=mid
        return mid


            
