class Solution:
    def containsDuplicate(self, nums: List[int]) -> bool:
        done=set()
        for i in nums:
            if i in done:
                return True
            done.add(i)
        return False
